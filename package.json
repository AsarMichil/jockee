{
  "name": "dj-turbo-repo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "packageManager": "bun@1.2.0",
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "immer": "^10.1.1"
  }
}
